---
title: 1. implement curry()
publishedDateTimeISO: 2026-01-07T10:07:00.000Z
tags:
  - bfedev
  - javascript
  - snippets
---
## [문제](https://bigfrontend.dev/problem/implement-curry)

함수 `fn`을 입력으로 받아, 커링된 함수를 return하는 `curry()` 함수를 구현해주세요.

## 선행 지식

### **`Function.prototype.call(thisArg, ...args)`**

* 함수를 **즉시 호출**한다.
* 첫 인자: 함수 내부의 this로 사용할 값
* 나머지 인자: 함수 인자로 그대로 전달

```js
fn.call(obj, a, b) // fn을 즉시 실행, this=obj
```

### **`Function.prototype.apply(thisArg, argsArray)`**

* call과 동일하게 **즉시 호출**한다.
* 차이: 인자를 **배열(또는 유사 배열)**&#xB85C; 한 번에 넘긴다.

```js
fn.apply(obj, [a, b])
```

### **`Function.prototype.bind(thisArg, ...args)`**

* 함수를 **즉시 실행하지 않고**, “새 함수(바인딩된 함수)”를 반환한다.
* 반환된 함수는:
  * this가 고정되고(기본적으로 바인딩된 thisArg)
  * 앞쪽 인자 일부가 미리 고정됨(partial application)

```js
const g = fn.bind(obj, a) // 실행 X
g(b) // 여기서 실행, fn(obj, a, b)
```

### **`function.length`**

* 함수 선언부의 **매개변수 개수**(arity)를 나타냄.

```js
function f(a, b, c) {}
f.length // 3
```

* 주의: 기본값이 있으면 그 지점부터는 length가 줄어듦

```js
(a, b = 1, c) => {}.length // 1
```

### **`함수의 this (일반 함수 vs 화살표 함수)`**

* **일반 함수**: 호출 방식에 따라 this가 결정됨 (obj.m(), call/apply/bind 등)
* **화살표 함수**: 자신만의 this가 없고, **정의 시점의 상위 스코프 this를 캡처**

## 풀이

<Collapsible>
  <PureMdx
    source="<CodeWithTooltips>

```js !code
// !tooltip[/function/] curry
function curry(fn) {
&nbsp;&nbsp;// !tooltip[/function/] curried
&nbsp;&nbsp;return function curried(...args){
&nbsp;&nbsp;&nbsp;&nbsp;// !tooltip[/fn\.length/] length
&nbsp;&nbsp;&nbsp;&nbsp;if(args.length >= fn.length){
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;return fn.call(this, ...args);
&nbsp;&nbsp;&nbsp;&nbsp;}
&nbsp;&nbsp;&nbsp;&nbsp;// !tooltip[/curried\.bind/] bind
&nbsp;&nbsp;&nbsp;&nbsp;return curried.bind(this, ...args)
&nbsp;&nbsp;}
}
```

## !!tooltips curry

curry는 wapper 역할만 하므로 화살표 함수여도 된다.

## !!tooltips curried

curried는 실제 호출되는 함수이므로 function으로 선언해서 this를 호출 시점의 this와 바인딩 시켜줘야 한다.

## !!tooltips length

함수 선언부의 파라미터 개수를 가져온다.

## !!tooltips bind

파라미터 개수가 다 채워지지 않았을 경우 지금까지 받은 파라미터를 미리 앞쪽에 고정시켜 curried의 파라미터를 누적한다.

</CodeWithTooltips>"
  />
</Collapsible>
