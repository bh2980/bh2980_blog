---
category: type-script
title: 108 - Trim
publishedDateTimeISO: 2026-01-12T12:08:00.000Z
tags:
  - type-challenges
  - typescript
---
## 질문

정확한 문자열 타입이고 양쪽 끝의 공백이 제거된 새 문자열을 반환하는 `Trim<T>`를 구현하십시오.

예시

```ts
type trimmed = Trim<'  Hello World  '> // 기대되는 결과는 'Hello World'입니다.

//!collapse(1:30) collapsed
/* _____________ 테스트 케이스 _____________ */
import type { Equal, Expect } from '@type-challenges/utils'

type cases = [
  Expect<Equal<Trim<'str'>, 'str'>>,
  Expect<Equal<Trim<' str'>, 'str'>>,
  Expect<Equal<Trim<'     str'>, 'str'>>,
  Expect<Equal<Trim<'str   '>, 'str'>>,
  Expect<Equal<Trim<'     str     '>, 'str'>>,
  Expect<Equal<Trim<'   \n\t foo bar \t'>, 'foo bar'>>,
  Expect<Equal<Trim<''>, ''>>,
  Expect<Equal<Trim<' \n\t '>, ''>>,
]
```

## 풀이

<Collapsible>
  <Code codeblock={{"lang":"typescript","value":"type Trim<S extends string> = \n    S extends `${' ' | '\\n' | '\\t'}${infer Last}` \n      ? Trim<Last> \n      : S extends `${infer First}${' ' | '\\n' | '\\t'}` \n          ? Trim<First> \n          : S;"}} />
</Collapsible>
