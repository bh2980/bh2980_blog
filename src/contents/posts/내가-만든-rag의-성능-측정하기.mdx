---
status: draft
category: development
title: 내가 만든 RAG의 성능 측정하기
publishedDateTimeISO: 2026-04-08T23:29:00.000Z
tags:
  - ai
policy:
  discriminant: normal
---
## 동작은 하는데 잘 동작하나요?

이전 글에서 블로그의 게시글을 바탕으로 원하는 답변을 생성하는 간단한 RAG를 직접 만들어봤습니다. 일단 질문을 던지면 답은 나옵니다. 하지만 몇 번 써보니 곧 의문이 들었습니다. 이 RAG가 정말 잘 작동하고 있는걸까요?

답이 그럴듯하게 보이는 것과 실제로 성능이 좋은 것은 다른 문제입니다. 직접 몇 번 질문해보는 것만으로는 검색이 잘 된 것인지, 아니면 모델이 그럴듯하게 답한 것인지 구분하기 어렵습니다. 그래서 이번 글에서는 직접 만든 RAG의 성능을 어떤 기준으로 측정할 수 있는지, 그리고 청킹 단위를 더 잘게 나눴을 때 그 지표가 어떻게 달라지는지 실험한 결과를 공유해보려고 합니다.

## 평가 기준 세우기

LLM은 글을 생성하는 모델입니다. 생성된 글을 채점하는 일은 생각보다 꽤 애매합니다. 마치 논술 채점과 비슷하죠. 하지만 다행히도 RAG에서는 이를 비교적 편하게 평가할 수 있는 라이브러리가 있으니, 바로 [Ragas](https://www.ragas.io/)입니다.

Ragas는 질문, 답변, 그리고 검색으로 가져온 문맥을 함께 보고 몇 가지 지표를 계산해 줍니다. 물론 이 점수만으로 성능을 단정할 수는 없습니다. 결국 이것도 완전히 객관적인 채점이라기보다는 논술 채점에 가까우니까요. 그래도 적어도 기준은 세울 수 있습니다. 저처럼 RAG가 잘 동작하는지 판단할 기준이 없는 상태였다면 꽤 유용하게 써볼 만한 도구입니다.

Ragas는 여러 가지 평가 지표를 제공하지만, 이번 글에서는 그중에서도 제가 이번 실험에 사용한 4가지 지표만 보겠습니다.

> 먼저, Answer Correctness는 최종 답변의 품질을, Faithfulness는 답변의 근거성을, Context Precision과 Recall은 검색 품질을 보는 지표입니다.

### Answer Correctness(답변 정확도)

$$
\text{Answer Correctness} = \text{사실 일치 점수} \times \text{의미적 유사도}
$$

Answer Correctness는 <u>AI의 사실 일치 여부과 임베딩 기반 의미 유사도를 함께 써서, 답변이 기준 정답과 얼마나 일치하는지 측정</u>합니다. 즉, 답변이 정답과 사실적으로 + 의미적으로 얼마나 일치하는가를 보는 최종 품질 지표입니다.

예를 들어 질문이 "왜 개발 블로그를 다시 만들었나요?"인데, 답변이 정답에 있는 핵심 이유들을 빠뜨렸다면 점수가 낮아집니다.

### **Faithfulness**(근거 충실성)

$$
\text{Faithfulness}
=
\frac{\text{검색된 청크로 뒷받침되는 답변 속 주장 수}}{\text{답변 속 전체 주장 수}}
$$

Faithfulness는 AI가 답변을 여러 개의 주장으로 분해하고, 이 주장 중 검색된 청크로 뒷받침 되는 개수를 세어 측정합니다. 즉, <u>답변이 검색된 문맥에 실제로 근거하고 있는가</u>를 봅니다. &#x20;

RAG를 쓰는 이유는 결국 내가 가진 문서를 바탕으로 답하게 만들기 위해서입니다. 그런데 검색된 문맥에는 없는 내용을 LLM이 그럴듯하게 덧붙여버리면, 답변이 자연스러워 보여도 좋은 답변이라고 보기 어렵습니다. 즉, 답변의 주장이 실제로 검색된 문맥에 의해 뒷받침되는지를 보는 지표라고 할 수 있습니다.

### **Context Precision**(문맥 정밀도)

$$
\text{Context Precision} = \text{관련 청크의 상위 배치 정도}
$$

Context Precision은 <u>검색된 청크들이 관련 있는 순서대로 잘 정렬되어 있는가</u>를 봅니다. 조금 더 정확히 말하면, 검색된 여러 청크 중에서 관련 있는 것들이 얼마나 위쪽에 배치되어 있는지를 보는 지표입니다.

RAG에서는 검색된 청크 여러 개를 LLM에게 한꺼번에 넘겨줍니다. 그런데 LLM은 주어진 문맥 전체를 균등하게 활용하지 않고, 앞에 놓인 내용을 더 많이 참고하는 경향이 있습니다. 때문에 관련 내용을 얼마나 앞에 배치했는지에 따라 점수가 달라집니다.

### **Context Recall**(문맥 재현율)

$$
\text{Context Recall}
=
\frac{\text{검색된 청크로 확인되는 정답 속 주장 수}}{\text{정답 속 전체 주장 수}}
$$

Context Recall는 <u>질문에 답하는 데 필요한 문맥을 빠뜨리지 않고 잘 가져왔는가</u>를 봅니다. 다만 이 지표는 정답이 미리 주어져 있어야 측정할 수 있습니다. 정답 속 주장들을 기준으로, 각각이 검색된 문맥에서 확인되는지를 역으로 체크하는 방식이기 때문입니다.

<IdeographicSpace />

저는 여기에 더해 레이턴시와 토큰 사용량, 평균 유사도도 함께 기록했습니다. RAGAS 지표가 '얼마나 잘 답했는가'를 본다면, 레이턴시와 토큰은 '얼마나 효율적으로 답했는가'를 보는 지표입니다. 청킹 방식을 바꿨을 때 성능만큼이나 효율도 달라질 수 있거든요.

## 테스트 세트 만들기

이제 평가 지표를 정했으니 테스트 세트를 만들어야 했습니다. 처음에는 Ragas에 테스트 세트를 자동으로 만들어주는 기능이 있어 이걸 쓰려고 했는데요. 막상 생성된 질문들을 보니 너무 일반적이었습니다. 블로그 글의 구체적인 내용을 제대로 테스트하기엔 부족하다는 느낌이 들었죠. 좋은 테스트 세트라면 '정말 제시된 문서를 읽어야만 알 수 있는' 질문들로 구성되어야 하는데, 자동 생성으로는 그 수준이 나오지 않았습니다.

질문 세트를 직접 만들어야 고민하고 있을 때, 제가 AI를 구독 중이라는 사실이 떠올랐습니다. 블로그 글이 5개밖에 없으니 차라리 Claude와 Codex에게 파일을 직접 읽혀서 질문을 만들게 하면 되지 않을까? 라는 생각이 들더군요.&#x20;

그렇게 최종적으로 40개 질문을 생성할 수 있었습니다. 5개 포스트를 고르게 커버했고, 단일 포스트에서 답을 찾는 질문 31개와 여러 포스트를 넘나드는 질문 9개로 구성됐습니다. 예를 들어 이런 질문입니다.

<Callout variant="info" title="글쓴이가 티스토리를 떠나 새 블로그를 만들기로 결심한 직접적인 계기는 뭐였어?">
  <IdeographicSpace />

  블로그 메인 화면에 개발 글과 단순 문제 풀이 기록이 한꺼번에 섞여 보였고, 처음 방문자에게 보여주고 싶은 인상과 달랐기 때문이다. 문제 풀이 글 자체가 싫었던 것은 아니지만, 플랫폼이 그 둘을 원하는 방식으로 구분해서 보여주지 못한다는 한계를 크게 느꼈다.
</Callout>

## 실험 1: 문서 단위 청킹

이제 테스트 세트까지 완성되었으니 실제 실험을 해볼 차례였습니다. 먼저 가장 기본이었던 문서 단위 청킹에서 성능을 측정해보기로 했습니다. 결과는 아래와 같습니다.

| 지표                 | 전체 40개 | 검색 성공 31개만 |
| ------------------ | ------ | ---------- |
| Faithfulness       | 0.937  | 0.951      |
| Context Precision  | 0.733  | 0.946      |
| Context Recall     | 0.742  | 0.957      |
| Answer Correctness | 0.431  | 0.517      |

먼저 성능 지표입니다. 검색 성공 시 Faithfulness, Context Precision, Context Recall은 모두 0.9 이상으로 나쁘지 않습니다. 다만 Answer Correctness는 0.517로, 검색이 돼도 정답과 완전히 일치하는 답변을 만들어내지는 못한다는 것을 알 수 있습니다. 즉, 검색만 되면 문맥 활용은 잘 하는데, 최종 답변의 정확도는 아직 개선이 필요하다는 것이겠죠.

| 지표       | 값        |
| -------- | -------- |
| 검색 실패    | 9 / 40개  |
| 평균 유사도   | 0.601    |
| 평균 레이턴시  | 15,982ms |
| 평균 입력 토큰 | 6,962    |

그렇다면 다른 지표도 봐볼까요? 무엇보다 눈에 띄는 건 검색 실패 사례인데요. 검색에 실패한 사례가 9개로 22%나 됩니다. 검색에 실패했다는 건 답변 생성과 별개로 모델이 답변을 생성할 수 있는 컨텍스트가 제공되지 않았다는 의미입니다. 즉, 유사도 검색에 실패했다는거죠.

왜 이런 일이 발생했을까요? 실패한 9개 질문을 살펴봤는데, 딱히 공통된 패턴을 찾기는 어려웠습니다. 포스트별로도 골고루 분포되어 있었고, 질문 유형도 다양했습니다.

다만 한 가지 추측해볼 수 있는 건, 포스트 전체를 하나의 청크로 임베딩해 전체적인 의미를 담게 되어 특정 섹션을 묻는 질문과는 유사도가 낮게 나올 수 있다는 것입니다. 실제로 평균 유사도가 0.6으로 threshold인 0.5보다 조금 높은 수준에 그쳤습니다.

그 밖에도 답변이 생성되기 까지 평균 16초 가량이 소요됐고, 답변을 생성하기 위해 약 7000 토큰 정도 소모하는 것을 알 수 있었습니다. 포스트 전체를 청크로 쓰다 보니 컨텍스트가 길어진 탓이라고 추측해볼 수 있겠네요.

<IdeographicSpace />

그렇다면 간단하게나마 결론을 내려볼 수 있겠죠. 문서 단위로 청크를 나누는 경우, 청킹 단위가 커서 특정 섹션과의 유사도 검색에 실패할 확률이 있다. 그렇다면 청킹 단위를 더 작게 세분화하면 어떨까요? 더 많이 성공하지 않을까요?

## 실험 2: 섹션 단위 청킹

이를 위해서 청킹 단위를 h2, h3 단위로 세분화해 보았습니다.

## 마무리

마무리 - 개선됐지만 남은 문제 다음 글(LightRAG) 예고
